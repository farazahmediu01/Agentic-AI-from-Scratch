# Your workspace — Chapter 3

Write your exercise and drill code here. This folder is wired into the quality gate, so `ruff`, `pyright` and `pytest` hold your code to the same bar as the reference builds.

Expected files by the end of the chapter:

| File | From |
|---|---|
| `triage_agent.py` | README Practice 10 — **mandatory**, the blank-file drill |
| `weather_agent.py` | EXERCISES Warm-ups 1–2 |
| `recipe_agent.py` + `test_recipe_replies.py` | EXERCISES Guided Build A |

Your own agent (Track 3) lives in `my_agent/` at the repo root, not here — it spans every chapter.

**Do not open `../solutions/` until you have attempted.** Reading the answer first feels efficient and teaches nothing.
